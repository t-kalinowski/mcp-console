#!/bin/sh

set -eu

cd "${0%/*}/.."

uv format || :

yamark format --wrap sentence || :

# `--all` follows the local Ark path dependencies into their workspace.
cargo fmt --package mcp-console || :

air format . || :

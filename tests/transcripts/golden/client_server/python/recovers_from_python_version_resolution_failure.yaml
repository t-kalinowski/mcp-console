---
!same-as tests/transcripts/golden/client_server/server/initializes_and_lists_tools.yaml
---
send:
  r: |
    worker_pid <- Sys.getpid()
    print(reticulate::py_require())
result:
  content:
    - type: text
      text: |
        Error: managed Python version resolution failed with exit status: 1: synthetic uv failure
        parse error: premature EOF

                             (right here) ------^

        Warning message:
        In system2(uv, args, ...) :
          running command ''<uv executable>' python list --all-versions --color never --output-format json --python-preference  only-managed' had status 99
  isError: false
---
send:
  r: identical(Sys.getpid(), worker_pid)
result:
  content:
    - type: text
      text: |
        [1] TRUE
  isError: false
...

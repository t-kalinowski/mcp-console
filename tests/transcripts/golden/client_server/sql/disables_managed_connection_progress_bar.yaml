---
!same-as tests/transcripts/golden/client_server/server/initializes_and_lists_tools.yaml
---
send:
  sql: SELECT current_setting('enable_progress_bar') AS enabled
result:
  content:
    - type: text
      text: |
        # A tibble: 1 × 1
          enabled
          <bool> 
        1 false  
  isError: false
...

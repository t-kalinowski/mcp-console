---
command:
  - mcp-console
  - sandbox
  - --
  - python
  - -c
  - |
    import signal
    import time

    signal.signal(signal.SIGHUP, signal.SIG_IGN)
    print("ready", flush=True)
    time.sleep(0.1)
    raise SystemExit(23)
terminal: closed after readiness
exit_code: 23
...

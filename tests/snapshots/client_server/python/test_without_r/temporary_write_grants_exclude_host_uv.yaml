---
options:
  exclude_tmpdir_env_var: false
special: ~
temporary_root: /var/tmp
stderr: >
  managed Python requires the default, :workspace, or :read-only sandbox
  policy without filesystem or native extensions; set python in
  .agents/console/config.yaml to use an existing environment
---
options:
  exclude_slash_tmp: false
special: ~
temporary_root: /tmp
stderr: >
  managed Python requires the default, :workspace, or :read-only sandbox
  policy without filesystem or native extensions; set python in
  .agents/console/config.yaml to use an existing environment
---
options: ~
special: ~
temporary_root: /var/tmp
stderr: >
  managed Python requires the default, :workspace, or :read-only sandbox
  policy without filesystem or native extensions; set python in
  .agents/console/config.yaml to use an existing environment
---
options: ~
special: ~
temporary_root: /tmp
stderr: >
  managed Python requires the default, :workspace, or :read-only sandbox
  policy without filesystem or native extensions; set python in
  .agents/console/config.yaml to use an existing environment
---
options: {}
special: tmpdir
temporary_root: /var/tmp
stderr: >
  managed Python requires the default, :workspace, or :read-only sandbox
  policy without filesystem or native extensions; set python in
  .agents/console/config.yaml to use an existing environment
---
options: {}
special: slash_tmp
temporary_root: /tmp
stderr: >
  managed Python requires the default, :workspace, or :read-only sandbox
  policy without filesystem or native extensions; set python in
  .agents/console/config.yaml to use an existing environment

---
stderr: >
  unsupported managed Python setting `UV_PYTHON_PREFERENCE`; use registry
  resolution with uv-managed Python, or set python in
  .agents/console/config.yaml to select an existing environment

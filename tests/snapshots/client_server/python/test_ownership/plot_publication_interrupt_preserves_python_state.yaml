---
!same-as MCP initialization for this execution mode
---
send:
  python: |
    import os
    import signal
    import sys
    import matplotlib.pyplot as plt

    original_process = os.getpid()
    original_object = object()


    def interrupt_publication(frame, event, argument):
        if event != "call" or frame.f_code.co_name != "call":
            return
        if frame.f_locals.get("operation") == "plot":
            sys.setprofile(None)
            signal.raise_signal(signal.SIGINT)


    _ = plt.plot([1, 2, 3])
    sys.setprofile(interrupt_publication)
result:
  content:
    - type: text
      text: |
        Traceback (most recent call last):
          File "<string>", line 322, in evaluate
          File "<string>", line 73, in call
          File "<mcp-console:python:e1>", line 15, in interrupt_publication
          File "<string>", line 94, in _interrupt
        KeyboardInterrupt
  isError: false
---
send:
  python: |
    assert os.getpid() == original_process
    assert original_object is not None
    6 * 7
result:
  content:
    - type: text
      text: |
        42
  isError: false

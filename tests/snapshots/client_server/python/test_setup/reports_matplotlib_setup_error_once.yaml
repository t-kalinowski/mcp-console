---
!same-as MCP initialization for this execution mode
---
send:
  r: |
    reticulate::py_run_string(
      r"---(
    import sys

    class FailingPyplot:
        def __setattr__(self, name: str, value: object) -> None:
            raise ValueError("matplotlib setup failed")

    sys.modules["matplotlib.pyplot"] = FailingPyplot()
    )---"
    )
result:
  content:
    - type: text
      text: "[done]"
  isError: false
---
send:
  python: raise AssertionError('failed setup ran the cell')
result:
  content:
    - type: text
      text: |-
        Error in py_eval_impl(code, convert) : 
          ValueError: matplotlib setup failed
        Run `reticulate::py_last_error()` for details.
        Python bridge failed during R evaluation
        [worker sideband read failed: worker sideband closed]
        [worker exited with status 1]
        [worker stopped: in-memory state lost]
        [starting new worker]
        [idle]
  isError: true

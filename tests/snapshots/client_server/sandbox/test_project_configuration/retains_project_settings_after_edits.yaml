---
!same-as tests/snapshots/client_server/server/test_tools/initializes_and_lists_tools.proxy.yaml
---
send:
  python: |
    import errno
    import os
    from pathlib import Path
    import socket

    host = Path(os.environ["MCP_CONSOLE_TEST_PROJECT"])
    configured = os.environ["MCP_CONSOLE_TEST_CONFIGURED"] == "1"
    assert "MCP_CONSOLE_SANDBOX_SETTINGS" not in os.environ
    assert "MCP_CONSOLE_SANDBOX_CONFIG" not in os.environ
    assert (os.environ.get("CODEX_NETWORK_PROXY_ACTIVE") == "1") == configured
    _ = (Path(os.environ["TMPDIR"]) / "private").write_text("private storage")
    _ = (host / "CLI cache" / "persistent").write_text("CLI grant")
    for name, allowed in (("output café 雪", configured), ("neighbor", False)):
        try:
            _ = (host / name / "created").write_text("project grant")
        except OSError as error:
            assert not allowed and error.errno in (errno.EPERM, errno.EACCES, errno.EROFS)
        else:
            assert allowed, name
    try:
        socket.create_connection(("127.0.0.1", int(os.environ["MCP_CONSOLE_TEST_PORT"])), timeout=2)
    except OSError:
        pass
    else:
        raise AssertionError("direct network unexpectedly allowed")
    os.chdir(host / "CLI cache")
    print("captured grants, proxy selection, and restricted network verified")
result:
  content:
    - type: text
      text: |
        captured grants, proxy selection, and restricted network verified
  isError: false
---
send:
  control: restart
result:
  content:
    - type: text
      text: |-
        [worker stopped: in-memory state lost]
        [starting new worker]
        [idle]
  isError: false
---
send:
  python: |
    import errno
    import os
    from pathlib import Path
    import socket

    host = Path(os.environ["MCP_CONSOLE_TEST_PROJECT"])
    configured = os.environ["MCP_CONSOLE_TEST_CONFIGURED"] == "1"
    assert "MCP_CONSOLE_SANDBOX_SETTINGS" not in os.environ
    assert "MCP_CONSOLE_SANDBOX_CONFIG" not in os.environ
    assert (os.environ.get("CODEX_NETWORK_PROXY_ACTIVE") == "1") == configured
    _ = (Path(os.environ["TMPDIR"]) / "private").write_text("private storage")
    _ = (host / "CLI cache" / "persistent").write_text("CLI grant")
    for name, allowed in (("output café 雪", configured), ("neighbor", False)):
        try:
            _ = (host / name / "created").write_text("project grant")
        except OSError as error:
            assert not allowed and error.errno in (errno.EPERM, errno.EACCES, errno.EROFS)
        else:
            assert allowed, name
    try:
        socket.create_connection(("127.0.0.1", int(os.environ["MCP_CONSOLE_TEST_PORT"])), timeout=2)
    except OSError:
        pass
    else:
        raise AssertionError("direct network unexpectedly allowed")
    os.chdir(host / "CLI cache")
    print("captured grants, proxy selection, and restricted network verified")
result:
  content:
    - type: text
      text: |
        captured grants, proxy selection, and restricted network verified
  isError: false
---
send:
  python: os._exit(23)
result:
  content:
    - type: text
      text: |-
        [worker sideband read failed: worker sideband closed]
        [worker exited with status 23]
        [worker stopped: in-memory state lost]
        [starting new worker]
        [idle]
  isError: true
---
send:
  python: |
    import errno
    import os
    from pathlib import Path
    import socket

    host = Path(os.environ["MCP_CONSOLE_TEST_PROJECT"])
    configured = os.environ["MCP_CONSOLE_TEST_CONFIGURED"] == "1"
    assert "MCP_CONSOLE_SANDBOX_SETTINGS" not in os.environ
    assert "MCP_CONSOLE_SANDBOX_CONFIG" not in os.environ
    assert (os.environ.get("CODEX_NETWORK_PROXY_ACTIVE") == "1") == configured
    _ = (Path(os.environ["TMPDIR"]) / "private").write_text("private storage")
    _ = (host / "CLI cache" / "persistent").write_text("CLI grant")
    for name, allowed in (("output café 雪", configured), ("neighbor", False)):
        try:
            _ = (host / name / "created").write_text("project grant")
        except OSError as error:
            assert not allowed and error.errno in (errno.EPERM, errno.EACCES, errno.EROFS)
        else:
            assert allowed, name
    try:
        socket.create_connection(("127.0.0.1", int(os.environ["MCP_CONSOLE_TEST_PORT"])), timeout=2)
    except OSError:
        pass
    else:
        raise AssertionError("direct network unexpectedly allowed")
    os.chdir(host / "CLI cache")
    print("captured grants, proxy selection, and restricted network verified")
result:
  content:
    - type: text
      text: |
        captured grants, proxy selection, and restricted network verified
  isError: false
---
send:
  control: restart
  requirements:
    python:
      - py-yaml12
result:
  content:
    - type: text
      text: |-
        [worker stopped: in-memory state lost]
        [starting new worker]
        [idle]
  isError: false
---
send:
  python: |
    import errno
    import os
    from pathlib import Path
    import socket

    host = Path(os.environ["MCP_CONSOLE_TEST_PROJECT"])
    configured = os.environ["MCP_CONSOLE_TEST_CONFIGURED"] == "1"
    assert "MCP_CONSOLE_SANDBOX_SETTINGS" not in os.environ
    assert "MCP_CONSOLE_SANDBOX_CONFIG" not in os.environ
    assert (os.environ.get("CODEX_NETWORK_PROXY_ACTIVE") == "1") == configured
    _ = (Path(os.environ["TMPDIR"]) / "private").write_text("private storage")
    _ = (host / "CLI cache" / "persistent").write_text("CLI grant")
    for name, allowed in (("output café 雪", configured), ("neighbor", False)):
        try:
            _ = (host / name / "created").write_text("project grant")
        except OSError as error:
            assert not allowed and error.errno in (errno.EPERM, errno.EACCES, errno.EROFS)
        else:
            assert allowed, name
    try:
        socket.create_connection(("127.0.0.1", int(os.environ["MCP_CONSOLE_TEST_PORT"])), timeout=2)
    except OSError:
        pass
    else:
        raise AssertionError("direct network unexpectedly allowed")
    os.chdir(host / "CLI cache")
    print("captured grants, proxy selection, and restricted network verified")
result:
  content:
    - type: text
      text: |
        captured grants, proxy selection, and restricted network verified
  isError: false
---
send:
  python: import yaml12; print(yaml12.__name__)
result:
  content:
    - type: text
      text: |
        yaml12
  isError: false

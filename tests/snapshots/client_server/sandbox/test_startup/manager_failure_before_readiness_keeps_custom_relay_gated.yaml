---
!same-as tests/snapshots/client_server/server/test_tools/initializes_and_lists_tools.yaml
---
send:
  r: echo echo
result:
  content:
    - type: text
      text: >-
        [failed to launch worker relay: sandbox manager did not become ready:
        failed to fill whole buffer; additionally failed to stop
        `/usr/bin/sandbox-exec` process group: Input/output error (os error 5);
        additionally failed to stop `/usr/bin/sandbox-exec`: Operation not
        permitted (os error 1)]
  isError: true
startup_gate_failure:
  manager: killed before readiness
  cleanup_signals: EIO for group, EPERM for direct root
  root_signal: SIGSTOP then SIGCONT
  relay_root: retired without executing the custom relay
  temporary_directory: preserved
---
send:
  r: echo echo
result:
  content:
    - type: text
      text: |
        zod: echo
  isError: false
startup_gate_recovery:
  custom_relay: executed only for the replacement generation
  private_gate: closed before relay exec

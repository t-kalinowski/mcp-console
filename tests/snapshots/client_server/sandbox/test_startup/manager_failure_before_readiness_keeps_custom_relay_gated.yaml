---
!same-as tests/snapshots/client_server/server/test_tools/initializes_and_lists_tools.yaml
---
send:
  r: echo echo
result:
  content:
    - type: text
      text: >-
        [failed to launch worker relay: sandbox manager did not become ready:
        failed to fill whole buffer]
  isError: true
startup_supervision_failure:
  manager: killed before readiness
  custom_relay: did not execute
  verified_cleanup: gated relay root and manager
---
send:
  r: echo echo
result:
  content:
    - type: text
      text: |
        zod: echo
  isError: false
startup_supervision_recovery:
  custom_relay: executed only for the replacement generation

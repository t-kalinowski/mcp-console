---
error: only one of `r`, `python`, or `sql` may be supplied
---
recovery: |
  zod: recovered
---
closed: >-
  MCPConsole is not connected; use `async with MCPConsole() as console` or
  call `await console.connect()` first
---
reconnected: |
  zod: reconnected

---
!same-as MCP initialization for this execution mode
---
send:
  python: import os; live_sentinel = 42; live_worker_pid = os.getpid()
result:
  content:
    - type: text
      text: "[done]"
  isError: false
---
send:
  python: failed_live_python_cell = True
  requirements:
    python:
      - numpy<0
result:
  content:
    - type: text
      text: |-
        managed Python resolution failed:
        resolver input:
        {
          "python": "==3.12.x",
          "packages": [
            "numpy",
            "pandas",
            "numpy<0"
          ],
          "python_version": [
            "==3.12.x"
          ]
        }
        uv output:
        × No solution found when resolving tool dependencies:
          ╰─▶ you require numpy<0, which does not allow any versions
  isError: true
---
send:
  python: |
    import os
    import yaml12

    (
        live_sentinel,
        os.getpid() == live_worker_pid,
        "failed_live_python_cell" not in globals(),
        yaml12.__name__,
    )
  requirements:
    python:
      - py-yaml12
result:
  content:
    - type: text
      text: |
        (42, True, True, 'yaml12')
  isError: false

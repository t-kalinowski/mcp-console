---
!same-as MCP initialization for this execution mode
---
send:
  python: import os; live_sentinel = 42; live_worker_pid = os.getpid()
result:
  content:
    - type: text
      text: "[done]"
  isError: false
---
send:
  python: |
    import _mcp_console_environment as environment

    original_check = environment._check_compatible


    def fail_compatibility(candidate):
        raise RuntimeError("synthetic live Python preparation failure")


    environment._check_compatible = fail_compatibility
result:
  content:
    - type: text
      text: "[done]"
  isError: false
---
send:
  python: failed_live_python_cell = True
  requirements:
    python:
      - py-yaml12
result:
  content:
    - type: text
      text: synthetic live Python preparation failure
  isError: true
---
send:
  python: environment._check_compatible = original_check
result:
  content:
    - type: text
      text: "[done]"
  isError: false
---
send:
  python: |
    import os
    import yaml12

    (
        live_sentinel,
        os.getpid() == live_worker_pid,
        "failed_live_python_cell" not in globals(),
        yaml12.__name__,
    )
  requirements:
    python:
      - py-yaml12
result:
  content:
    - type: text
      text: |
        (42, True, True, 'yaml12')
  isError: false

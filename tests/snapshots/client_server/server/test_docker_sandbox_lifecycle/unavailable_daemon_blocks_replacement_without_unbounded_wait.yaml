---
send:
  r: "42"
result:
  content:
    - type: text
      text: |
        [1] 42
  isError: false
---
send:
  control: restart
  r: must_not_run <- TRUE
result:
  content:
    - type: text
      text: |-
        [worker launcher exited with status 1; additionally failed to retire worker I/O: Docker Sandbox microVM '<owned-microvm>': target retirement is unconfirmed (transport exit is not a cleanup barrier); this session cannot start a replacement]
        session restart did not complete; cell was not run
  isError: true
---
send:
  control: restart
  r: must_not_run <- TRUE
result:
  content:
    - type: text
      text: "[worker is shutting down]"
  isError: true
---
send:
  r: "42"
result:
  content:
    - type: text
      text: |
        [1] 42
  isError: false
---
send:
  control: restart
  r: must_not_run <- TRUE
result:
  content:
    - type: text
      text: |-
        [worker launcher exited with status 1; additionally failed to retire worker I/O: Docker Sandbox microVM '<owned-microvm>': target retirement is unconfirmed (transport exit is not a cleanup barrier); this session cannot start a replacement]
        session restart did not complete; cell was not run
  isError: true
---
send:
  control: restart
  r: must_not_run <- TRUE
result:
  content:
    - type: text
      text: "[worker is shutting down]"
  isError: true

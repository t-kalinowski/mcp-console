---
inherit_environment: true
stdout_json:
  inherited: from launcher
  marker: |-
    雪, café; 'quoted' "double" $() `literal` \ newline
    end
  value: |-
    雪, café; 'quoted' "double" $() `literal` \ newline
    end
  arguments:
    - |-
      雪, café; 'quoted' "double" $() `literal` \ newline
      end
    - --config-env
    - ANOTHER_POLICY
    - --bootstrap-fd
    - "3"
---
inherit_environment: false
stdout_json:
  inherited: ~
  marker: |-
    雪, café; 'quoted' "double" $() `literal` \ newline
    end
  value: |-
    雪, café; 'quoted' "double" $() `literal` \ newline
    end
  arguments:
    - |-
      雪, café; 'quoted' "double" $() `literal` \ newline
      end
    - --config-env
    - ANOTHER_POLICY
    - --bootstrap-fd
    - "3"

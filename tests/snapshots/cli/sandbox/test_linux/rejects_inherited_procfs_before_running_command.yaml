---
command:
  - mcp-console
  - sandbox
  - --
  - /bin/echo
  - must not run
environment: fresh procfs mounts denied by an outer namespace
exit_code: 1
stdout: ""
stderr: |
  native target setup requires namespace-local procfs

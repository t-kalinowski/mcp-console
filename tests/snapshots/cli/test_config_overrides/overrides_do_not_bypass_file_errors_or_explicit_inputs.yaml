---
arguments:
  - serve
  - --no-sandbox
  - -c
  - "sandbox={} "
stderr: >
  .agents/console/config.yaml: while parsing a node, did not find expected
  node content at byte 10 line 2 column 1
---
arguments:
  - -c
  - extends=:workspace
  - sandbox
  - --config-env
  - TEST_POLICY
  - --
  - /usr/bin/true
stderr: |
  configuration overrides cannot be combined with --config-env or --settings-env
---
arguments:
  - sandbox
  - --settings-env
  - TEST_POLICY
  - -c
  - extends=:workspace
  - --
  - /usr/bin/true
stderr: |
  configuration overrides cannot be combined with --config-env or --settings-env

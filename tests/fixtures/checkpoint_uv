#!/usr/bin/env python3

import os
import sys


def checkpoint() -> None:
    argument = os.environ["MCP_CONSOLE_TEST_UV_CHECKPOINT_ARGUMENT"]
    if not any(argument in value for value in sys.argv[1:]):
        return

    claim = os.environ["MCP_CONSOLE_TEST_UV_CHECKPOINT_CLAIM"]
    try:
        descriptor = os.open(claim, os.O_CREAT | os.O_EXCL | os.O_WRONLY, 0o600)
    except FileExistsError:
        return
    os.close(descriptor)

    started = os.environ["MCP_CONSOLE_TEST_UV_STARTED"]
    descriptor = os.open(started, os.O_WRONLY | os.O_NONBLOCK)
    with os.fdopen(descriptor, "wb", buffering=0) as stream:
        stream.write(b"1")

    release = os.environ["MCP_CONSOLE_TEST_UV_RELEASE"]
    with open(release, "rb", buffering=0) as stream:
        assert stream.read(1) == b"1"


def main() -> None:
    checkpoint()
    executable = os.environ["MCP_CONSOLE_TEST_REAL_UV"]
    os.execv(executable, [executable, *sys.argv[1:]])


if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        raise KeyboardInterrupt from None
